{
  "name": "express",
  "version": "1.0.0",
  "description": "express tutorial",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index",
    "json-server": "json-server --port 5050 --watch db.json",
    "dev": "nodemon index.json",
    "con": "concurrently \"npm run json-server\" \"npm run dev\" ",
    "altCon": "concurrently \"npm run json-server\" \"npm start\" "
    
  },
  "author": "hammed",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
  },
  "engines":{
    "node":"14.2"
  }
}
